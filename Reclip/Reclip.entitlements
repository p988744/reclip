<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- App Sandbox（Mac App Store 必要） -->
    <key>com.apple.security.app-sandbox</key>
    <true/>

    <!-- 網路存取（Claude API、Ollama、模型下載） -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- 使用者選擇的檔案讀寫（音訊匯入/匯出） -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- 下載資料夾讀寫（預設匯出位置） -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>

    <!-- iCloud 文件存取 -->
    <key>com.apple.security.application-groups</key>
    <array>
        <string>group.com.reclip.app</string>
    </array>

    <!-- CloudKit 容器（iCloud 同步） -->
    <key>com.apple.developer.icloud-container-identifiers</key>
    <array>
        <string>iCloud.com.reclip.app</string>
    </array>

    <!-- iCloud Key-Value 儲存（設定同步） -->
    <key>com.apple.developer.ubiquity-kvstore-identifier</key>
    <string>$(TeamIdentifierPrefix)com.reclip.app</string>

    <!-- CloudKit 服務 -->
    <key>com.apple.developer.icloud-services</key>
    <array>
        <string>CloudKit</string>
        <string>CloudDocuments</string>
    </array>

    <!-- Hardened Runtime（公證必要） -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <false/>

    <!-- 禁用 JIT（安全性） -->
    <key>com.apple.security.cs.allow-jit</key>
    <false/>

    <!-- 禁用 DYLD 環境變數（安全性） -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <false/>
</dict>
</plist>
